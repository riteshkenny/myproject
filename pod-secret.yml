apiVersion: v1
kind: Pod
metadata:
  name: pod-secret1
spec:
  containers:
    - image: nginx
      name: myc1
      volumeMounts:
        - name: foo
          mountPath: "/etc/foo"
          readOnly: true

  volumes:
    - name: foo
      secret:
        secretName: secret3
